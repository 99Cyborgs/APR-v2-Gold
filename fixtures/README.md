# Fixtures

- `inputs/` contains the canonical local manuscript fixtures used by CLI examples and the gold-set harness.
- `external_packs/` contains a minimal advisory-only external pack scaffold used for pack loading and integration tests.
