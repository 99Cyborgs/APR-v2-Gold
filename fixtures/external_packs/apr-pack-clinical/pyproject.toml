[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apr-pack-clinical"
version = "0.1.0"
description = "APR v2 advisory clinical pack fixture"
requires-python = ">=3.12"
