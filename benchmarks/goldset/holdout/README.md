# Holdout

APR v2 Gold keeps the `holdout` stratum explicit in the manifest, but the public repo currently has no active untuned holdout fixtures.

Until real public holdout material exists, holdout remains reserved rather than silently reusing tuned gold fixtures.
