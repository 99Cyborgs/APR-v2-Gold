# APR v2 Hardening Evidence Index

Review anchor:
- GitHub connector repository: `99Cyborgs/APR-v2-Gold`
- Reviewed working state: remote branch `codex/full-worktree-publish`, which is ahead of default branch `main` by 4 commits and contains benchmark-governance surfaces explicitly named in the mission.
- Baseline validation run before authoring these artifacts: `python scripts/validate_contract.py`, `python scripts/validate_goldset.py`, and `python -m pytest tests/contract tests/goldset/test_goldset_runner.py tests/regression/test_cli_smoke.py tests/surface_lock/test_output_surfaces.py -q`, all passing.

| File path | Role in architecture | Why it matters to hardening | Confirmed facts extracted from it | Contradictions or drift notes | Confidence |
| --- | --- | --- | --- | --- | --- |
| `README.md` | Repo identity and current-state summary | Publicly states package, CLI, contract, benchmark, and holdout story | Names package `apr-v2`; names CLI `apr`; says runtime lives in `src/apr_core/`; says active contract is `contracts/active/`; now points benchmark execution at split dev/holdout manifests and describes holdout as active blind evaluation | Authoritative README drift was corrected on 2026-04-09; residual stale wording may still exist only in generated or historical surfaces such as `src/apr_v2.egg-info/`, `dist/`, or old `output/` artifacts | High |
| `pyproject.toml` | Packaging and entrypoint truth surface | Confirms installable package identity and exposed console scripts | Project name is `apr-v2`; Python `>=3.12`; dependencies limited to `jsonschema` and `PyYAML`; `apr` script points to `apr_core_bootstrap:main` | Exposes helper console scripts beyond `apr`, so release/readiness docs must stay clear about supported vs helper CLIs | High |
| `setup.py` | Release version authority bridge | Prevents package version from drifting from active manifest | Reads version directly from `contracts/active/manifest.yaml` | Version lock depends on manifest regex and is duplicated by tests/docs | High |
| `src/apr_core_bootstrap.py` | Bootstrap entrypoint | Confirms bootstrap path and import strategy | Adds `src` to `sys.path` and dispatches to `apr_core.cli:main` | None observed | High |
| `src/apr_core/cli.py` | Supported CLI contract | Central user-facing execution and validation surface | Subcommands are `doctor`, `readiness`, `audit`, `review`, `render`, `goldset`, and `packs`; `doctor` validates active contracts, benchmark schemas, manifests, and a sample runtime audit without requiring a clean worktree; `readiness` reuses the same checks but fail-closes with `release_readiness_requires_clean_worktree`; `cmd_goldset()` now snapshots summary/governance/ledger outputs and restores the old-good state if ledger append fails after the final summary bundle is written | Local installed helper entrypoints may still fail outside source-invoked validation if the environment lacks the bootstrap import path; the split between runtime health and release cleanliness is now explicit in the supported CLI contract | High |
| `src/apr_core/pipeline.py` | Core deterministic audit pipeline | Defines live control flow, canonical record assembly, and output validation | Performs input schema validation before normalization and output schema validation before return; fixed stage order ends with `PACKS_EXECUTED`; `CanonicalAuditRecord` is emitted before rendering | Processing-state alignment is now explicit across policy, schema, and docs; replay-fingerprint expansion remains separate work | High |
| `src/apr_core/models.py` | Canonical in-memory transport shells | Confirms `CanonicalAuditRecord` is the only runtime truth object and what fields it currently carries | `CanonicalAuditRecord` includes contract/policy versions, semantic blocks, pack surfaces, decision, provenance, and rendering | Dataclass field list is open-ended relative to semantic governance; schema remains authoritative | High |
| `src/apr_core/policy/loader.py` | Active contract loader | Confirms runtime only loads `contracts/active/` | Manifest, policy layer, input schema, and canonical schema are loaded from `contracts/active/` with `lru_cache` | No code path loads `contracts/legacy/`; none observed | High |
| `src/apr_core/ingest/normalize.py` | Boundary normalization and input sufficiency grading | Shows what the runtime actually normalizes and grades | Input normalization is local and string/list based; `grade_input_sufficiency` emits `low/medium/high` and `abstract_only/partial_manuscript/complete_manuscript` | Input schema allows many nullable strings but does not encode higher-level sufficiency rules | High |
| `src/apr_core/parse/claim_extraction.py` | Claim and anchor extraction | Shapes claim recovery, contradiction flags, and anchor index | Central claim, novelty delta, hard object, decisive support object, contradiction flags, and anchor index are derived heuristically from visible text surfaces | Heuristic outputs are contract-bearing, but only some are explicitly benchmarked | High |
| `src/apr_core/classify/classification.py` | Article/claim/domain/outlet classification | One of the main drift points between doctrine and runtime | Runtime hard-codes article types, claim types, outlet-profile heuristics, and domain-module mapping; article-claim mismatch is derived here | Policy layer duplicates these taxonomies; canonical schema leaves classification fields as unconstrained strings | High |
| `src/apr_core/reviewability/gate.py` | Early assessability gate | Confirms reviewability is a hard prerequisite before scientific-record judgment | Reviewability status is binary `pass/fail`; checks are fixed and named | None observed | High |
| `src/apr_core/transparency/availability.py` | Transparency readiness sub-assessment | Needed for contract/docs/runtime alignment and benchmark coverage planning | Infers expected data/code/materials surfaces from article type and domain; emits `not_applicable/missing/incomplete/declared` | `docs/SPEC_IMPLEMENTATION_MATRIX.md` explicitly says transparency sub-assessment is implemented but not directly benchmarked | High |
| `src/apr_core/integrity/signals.py` | Integrity escalation gate | Confirms integrity signals are scoped and local | Emits `clear/flagged/escalate`; human escalation is tied to explicit phrase rules | No live external policy service or provider involvement exists | High |
| `src/apr_core/structure.py` | Research-spine assessment | Important for structural hardening and drift analysis | Computes eight research-spine signals and thresholds from policy-layer config | Thresholds live in policy, but code computes the semantics | High |
| `src/apr_core/calibration.py` | Claim-evidence calibration | Important contract-bearing semantic gate and benchmark surface | Uses policy-layer thresholds for `watch/fail/fatal/aligned`; affects recommendation path | None observed | High |
| `src/apr_core/adversarial.py` | Adversarial-resilience analysis | Active hardening surface already present in runtime | Emits flag families and `clear/watch/downgraded/blocked` based on local text markers | Strong existing surface that should be preserved and only tightened additively | High |
| `src/apr_core/scientific_record/gate.py` | Scientific-record gate | Central recommendation boundary and criterion taxonomy | Emits criterion objects, fatal vs repairable failures, and `pass/borderline/repairable_fail/fatal_fail` | Failure names and severities are partly free strings across runtime and benchmark surfaces | High |
| `src/apr_core/venue/routing.py` | Venue routing boundary | Confirms scientific-record gate precedes venue routing | Venue routing never runs as a replacement for scientific validity; blocked state is explicit | `routing_state` is currently a free string in the canonical schema | High |
| `src/apr_core/editorial_first_pass.py` | Editorial simulation layer | Active but non-gating benchmark and output surface | Computes score components and desk-reject probability from local content | Benchmark harness already guards that editorial scoring stays non-gating | High |
| `src/apr_core/rehabilitation/planner.py` | Ranked repair planning | Hardening must preserve as additive downstream guidance | Produces `development_track`, minimum evidence package, and next actions ranked after core semantics | Rehab ranking is emitted but not benchmark-frozen today | High |
| `src/apr_core/render/markdown.py` | Canonical-output renderer | Confirms render restraint boundary | Renders only from canonical record fields; no runtime recomputation | Direct function use relies on caller validation; CLI path validates before render | High |
| `src/apr_core/packs/loader.py` | Pack trust boundary | Primary surface for pack hardening | Loads packs by explicit path; canonicalizes and dedupes requested pack roots; records `resolved_repo_root`, `manifest_path`, and `manifest_sha256`; normalizes results including advisory-only fatal-gate scopes; records failures rather than masking them; pack outputs are additive | Temporary `sys.path` mutation is still the remaining import-boundary seam, and pack fatal-gate semantics remain advisory metadata only | High |
| `src/apr_core/packs/protocol.py` | Pack API contract | Defines minimal pack runtime interface | `PackSpec` carries identity, advisory flag, domains, runner, repo root, and module metadata | No stronger runtime contract on advisory fields beyond schema object shape | High |
| `src/apr_core/providers/protocol.py` | Latent provider seam | Important to keep inactive unless formally admitted | Only defines a future `StructuredProvider` protocol | Confirms provider seam is dormant, not active runtime; admission criteria now live in doctrine docs and seam-lock tests rather than runtime hooks | High |
| `src/apr_core/adapters/__init__.py` | Latent adapter seam | Important to quarantine | Explicitly says adapters are reserved for explicit future use and current runtime is local and deterministic | Confirms adapter seam is dormant, not active runtime; future admission now requires explicit docs-and-tests changes | High |
| `contracts/active/manifest.yaml` | Runtime manifest authority | Highest runtime contract surface after GitHub repo state | Contract version `2.1.0`; runtime-loadable contracts only `[2.1.0]`; legacy runtime loading is false; release version source is the active manifest | None observed at runtime boundary | High |
| `contracts/active/policy_layer.yaml` | Doctrine and taxonomy surface | Main doctrinal and enum duplication surface | Carries doctrine, compatibility flags, taxonomies, thresholds, processing states, recommendation states, and pack doctrine; processing states now stop at `PACKS_EXECUTED` in lockstep with runtime provenance | Still duplicates runtime state lists, so lockstep tests remain necessary when policy or pipeline states change | High |
| `contracts/active/audit_input.schema.json` | Input boundary schema | Confirms fail-closed input boundary | Only `audit_mode` is required; other fields are nullable and closed to additional properties | Higher-level sufficiency logic is outside schema by design | High |
| `contracts/active/canonical_audit_record.schema.json` | Canonical output schema | Main contract-closure surface for hardening | Top-level record is closed and required fields are explicit; recommendation enum is defined in `$defs`; pack result shape is closed; classification taxonomies and `venue.routing_state` are enum-closed to the active runtime vocabularies | Provenance still lacks the stronger replay fingerprints tracked separately in hardening work | High |
| `benchmarks/goldset_dev/manifest.yaml` | Development benchmark case registry | Confirms active development harness shape | Manifest version `4.0.0`; contract version `2.1.0`; 17 active `dev` cases across `core_gold` and `stress_gold` | Current manifest matches active contract, but no code-side guard forces future matches | High |
| `benchmarks/goldset_holdout/manifest.yaml` | Holdout benchmark case registry | Confirms active holdout path exists now | Manifest version `4.0.0`; contract version `2.1.0`; 3 active `holdout` cases | Authoritative docs now match this active holdout state, and holdout truth/isolation is guarded by dedicated leakage, split-isolation, and CLI smoke tests | High |
| `benchmarks/goldset/README.md` | Benchmark operator-facing docs | Important documentation truth surface | Describes harness, shared schemas, ledger path, and now points operators at `goldset_dev/manifest.yaml` and `goldset_holdout/manifest.yaml` | Shared benchmark docs were corrected on 2026-04-09; remaining hardening need is docs lockstep enforcement rather than more manual truth fixes on this file | High |
| `benchmarks/goldset/holdout/README.md` | Compatibility note for legacy holdout subdirectory | Clarifies that the placeholder directory does not own active holdout execution | Describes `benchmarks/goldset_holdout/manifest.yaml` as the active holdout lane and marks this subdirectory as compatibility-only | Added to remove stale implication that holdout was still reserved under `benchmarks/goldset/holdout/` | High |
| `benchmarks/goldset/schemas/manifest.schema.json` | Benchmark manifest contract | Governs case-registry validity | Requires manifest version, contract version, strata, and cases; allowlists exact expected paths | `contract_version` is only typed as string; schema does not bind it to the active contract version | High |
| `benchmarks/goldset/schemas/summary.schema.json` | Benchmark summary contract | Governs public/private benchmark output surface | Summary is schema-validated and carries replay-critical fields including manifest path/hash, contract/policy/schema digests, runtime identity, repo state, governance, gates, and calibration export fields | Important additive hardening surface; namespace closure is enforced by runner validation rather than schema enums alone | High |
| `benchmarks/goldset/schemas/ledger_entry.schema.json` | Durable regression-history contract | Governs JSONL ledger rows | Ledger rows mirror the replay envelope with governance, case deltas, recommendation changes, gates, runtime identity, repo state, and case outcomes | Append path is direct JSONL append, not atomic replace | High |
| `src/apr_core/goldset/runner.py` | Benchmark execution, ledger, holdout masking, and drift engine | First-class architecture and major hardening surface | Loads manifests and schemas; computes drift and governance layers; validates summary before return; appends validated ledger entries; isolates development vs holdout evaluation modes; enforces manifest-vs-active-contract parity in `_assert_manifest_contract_parity()`; rejects unknown error classes and governance reason codes before durable artifact emission | Direct JSONL append is not atomic; benchmark governance strings and enums are duplicated in code, so additive namespace changes still require coordinated test/doc updates | High |
| `src/apr_core/goldset/governance/governance_router.py` | Benchmark governance-layer admission | Important for latent-layer hardening | Governance layers are additive and optional; surface-contract, leakage, identifiability, and invariance layers are isolated here | Strong pattern to preserve: benchmark-only layers do not rewrite live runtime decisions | High |
| `src/apr_core/goldset/governance/surface_contract.py` | Score namespace isolation guard | Important for fail-closed benchmark surface usage | Explicitly guards mixed `scientific_score` and `scientific_vector` usage and validates schema fields exist | Good existing hardening surface; candidate model for broader lockstep checks | High |
| `src/apr_core/utils.py` | Local durable-write helper layer | Primary evidence for atomic canonical/report persistence | `write_json()` and `write_text()` both route through `_atomic_write_text()`, `write_text_bundle()` installs multi-file outputs with rollback, and `append_jsonl_atomic()` rewrites JSONL through the same atomic helper | Current helper path is atomic, but future direct-write call sites would reopen the risk | High |
| `scripts/validate_contract.py` | Contract validation entrypoint | One of the canonical repo-level gates | Validates schemas, fixture inputs, runtime audit, explicit version pins, policy/runtime taxonomy lockstep, and canonical-schema enum lockstep for fixed namespaces; validates pack-enabled theory fixture too | Hard-codes version `2.1.0`; additive taxonomy changes still require coordinated updates to the policy list, runtime constants, and tests | High |
| `scripts/validate_goldset.py` | Benchmark validation entrypoint | Canonical benchmark-structure gate | Validates manifest, summary, and ledger schemas; manifest loadability flows through `load_goldset_manifest()`, so contract-version parity is enforced for dev, holdout, and explicit manifests | Still relies on runner-owned parity logic rather than an independent script-local assertion | High |
| `scripts/validate_repo_lockstep.py` | Unified repo lockstep gate | Collapses the minimum pre-merge contract, benchmark, doc-lockstep, and CLI smoke path into one entrypoint | Runs `validate_contract.py`, `validate_goldset.py`, and the targeted pytest slice that covers CLI smoke plus authoritative holdout-doc checks; the same script is invoked from CI, `Makefile`, and developer docs | Intentionally narrower than full pytest, so broader suites still matter for release-level confidence | High |
| `scripts/build_release.py` | Release packaging gate | Important for release readiness hardening | Requires git repo and clean worktree; version comes from active manifest; excludes `dist`, `output`, `reports`, `.apr`, caches | Release contents are path-based and deterministic; `tests/regression/test_release_contract.py` now locks the exclusions and packaging identity around it | High |
| `.github/workflows/ci.yml` | CI truth surface | Defines merge-time validation behavior | Installs editable package, runs both validation scripts, full pytest, `apr-goldset`, and `apr-holdout` | CI already treats benchmark and contract surfaces as first-class; any hardening plan should extend, not bypass, this pattern | High |
| `tests/contract/test_contract_manifest.py` | Contract lock tests | Evidence that version and fixed-taxonomy pins are intentional | Locks active contract and policy versions to `2.1.0`, checks schema validity, and now locks policy/runtime parity for article, claim, domain, outlet, recommendation, and processing-state taxonomies | Version pin will need coordinated update in future migrations | High |
| `tests/goldset/test_goldset_runner.py` | Benchmark governance regression suite | Strong evidence that goldset is architecture, not utility | Covers ledger writes, replay-envelope fields, holdout redaction, drift gates, editorial non-interference, calibration exports, namespace validation, and governance flags | Suite now locks manifest parity, replay-envelope presence, and fail-closed namespace validation, but it is not a crash-safety suite for append semantics | High |
| `tests/goldset/test_holdout_leakage.py` | Holdout leakage hardening suite | Evidence that blind holdout output does not leak expected labels or exact loss surfaces | Verifies obfuscated holdout results collapse expected surfaces, redact recommendation details, and resist repeated probing over nearby losses | Focuses on public-surface masking, not on development/holdout history separation | High |
| `tests/goldset/test_holdout_split_isolation.py` | Holdout truthfulness and split-isolation suite | Evidence that holdout runs stay isolated from development baselines and that authoritative docs stay truthful | Verifies holdout ledger entries do not seed development baselines or case histories and locks benchmark-doc references to the active dev/holdout manifests while banning stale manifest paths | This suite is the authoritative-doc truth gate, not a full benchmark replay suite | High |
| `tests/regression/test_repo_lockstep.py` | Repo-lockstep smoke test | Evidence that the single documented repo-level validation entrypoint actually works | Invokes `scripts/validate_repo_lockstep.py` with the same source-root wiring used by local operators and asserts the success banner | The script intentionally covers a minimum sufficient gate, not every slower replay or adversarial suite | High |
| `tests/regression/test_atomic_writes.py` | Atomic-write and rollback regression suite | Evidence that canonical/report helper writes and deferred goldset persistence fail closed on replacement failure | Proves JSON writes, markdown writes, multi-file bundle writes, and deferred goldset summary/governance/ledger rollback preserve prior on-disk state when replacement or ledger append fails | Does not prove crash recovery across process termination mid-sequence; it proves the repo's current exception-path rollback semantics | High |
| `tests/regression/test_release_contract.py` | Release/package truth lock tests | Evidence that package identity and release doctrine are fail-closed | Locks package name, bootstrap entrypoint, helper readiness scripts, manifest-sourced versioning, and release-bundle exclusions | Clean-tree release smoke still requires a clean worktree, so dirty local sessions should use this file plus docs for package-truth verification | High |
| `tests/regression/test_surface_isolation.py` | Seam and extension-governance lock tests | Evidence that dormant extension seams stay inactive and that governing docs stay explicit | Locks provider/adapter seam dormancy and the required extension-governance text in `docs/MIGRATION_POLICY.md`, `docs/PACK_INTERFACE.md`, `docs/EXECUTION_MODEL.md`, and `docs/REPO_CHARTER.md` | This test guards doctrine drift for extension boundaries, not README/benchmark manifest wording | High |
| `tests/surface_lock/test_output_surfaces.py` | Canonical/output surface lock tests | Main guard against renderer and output drift | Locks top-level canonical keys, decision keys, transparency keys, scientific-record criteria order, markdown section order, and pack output surface behavior | Any provenance expansion or schema closure work must update this file intentionally | High |
| `docs/CANONICAL_AUDIT_RECORD.md` | Canonical-truth doctrine doc | Must stay synchronized with schema and runtime | Says every downstream consumer reads `CanonicalAuditRecord`, renderers may not redefine field meaning, and `pack_results[*].fatal_gates` remain advisory pack requests rather than core fatal gates | Good alignment with runtime and manifest | High |
| `docs/PACK_INTERFACE.md` | Pack boundary doctrine doc | Important for pack hardening and restraint | States packs are explicit-path advisory extensions, may not redefine core semantics, and that scoped fatal-gate requests are advisory only and do not participate in core recommendation selection | Current doctrine now matches runtime behavior and the pack regression package | High |
| `docs/BENCHMARK_POLICY.md` | Benchmark governance authority doc | Main benchmark doctrine surface | Correctly points to dev and holdout manifests, schemas, gate rules, and ledger rules | Largely aligned with current branch and should remain benchmark-governance authority | High |
| `docs/SPEC_IMPLEMENTATION_MATRIX.md` | Spec-vs-runtime truth surface | Important for docs/reality lockstep | Correctly distinguishes implemented-and-benchmarked vs implemented-but-not-benchmarked surfaces; now includes the blind holdout benchmark lane as implemented and benchmarked | Authoritative matrix drift on holdout status was corrected on 2026-04-09; remaining risk is lack of automated docs lockstep checks | High |
| `docs/EXECUTION_MODEL.md` | Runtime boundary doctrine | Important for absence proofs | States deterministic, local-only execution and no external model provider in initial build | Matches code search: no active network/provider libraries found in `src/`, `scripts/`, `tests/`, or contract surfaces | High |
| `docs/SYSTEM_BOUNDARY.md` | Scope boundary doc | Needed to keep hardening additive | Explicitly excludes journal workflow automation, reviewer management, live editorial prioritization, misconduct adjudication, and renderer redefinition of canonical meaning | Aligns with runtime and should constrain future hardening | High |
